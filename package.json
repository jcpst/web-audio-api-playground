{
  "name": "audio",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c",
    "lint": "standard",
    "test": "npm run lint -s && npm run unit -s",
    "unit": "jest",
    "watch": "nodemon --watch src --exec \"npm run build -s\""
  },
  "devDependencies": {
    "jest": "^19.0.2",
    "nodemon": "^1.11.0",
    "rollup": "^0.41.6",
    "rollup-plugin-buble": "^0.15.0",
    "standard": "^10.0.2"
  },
  "dependencies": {}
}
